# Initilisation

## Python environment

```
python -m venv venv
```

## Contributing (reminder for myself)

Before contributing to repository:
```
git config --global user.name <username>
git config --global user.email <email>
```